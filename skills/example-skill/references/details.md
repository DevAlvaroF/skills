# Extended details

Reference files are loaded on demand, not at session start. Use them for
long checklists, API tables, or examples that would otherwise bloat every
session that merely *considers* this skill.

Reference them from SKILL.md by relative path so the agent knows they exist.
